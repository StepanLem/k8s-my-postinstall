apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc-retain
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-retain
  resources:
    requests:
      storage: 1Gi

